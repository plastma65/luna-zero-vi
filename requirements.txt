# Chang 1 - tokenizer. Co y giu mong: chua cai torch o buoc nay.
#
# LUU Y: file nay phai la ASCII thuan. pip doc requirements.txt bang encoding
# mac dinh cua he thong (cp1252 tren Windows), khong phai UTF-8, nen mot dau
# tieng Viet trong comment lam pip chet voi UnicodeDecodeError.
# Giai thich day du bang tieng Viet nam trong README.md va CLAUDE.md.
tokenizers>=0.20,<1.0
datasets>=2.19
tqdm>=4.66
numpy>=1.26
huggingface_hub>=0.24

# torch KHONG nam o day: no gan voi phan cung.
#   May co GPU  : pip install torch --index-url https://download.pytorch.org/whl/cu124
#   May khong GPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Dung `pip install torch` tran tren Windows se lay ban CPU-only, chay duoc nhung
# khong dung toi GPU va cham hon khoang 50 lan.

# Cong cu phat trien
ruff>=0.6
black>=24.8
pytest>=8.3

# Chang 4 - export inference-only weights in a Hugging Face safe format.
safetensors>=0.4,<1.0
